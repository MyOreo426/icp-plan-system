# 🎉 内控计划共享编辑系统 - 免费云部署指南

> 本指南面向**非技术人员**，用最通俗的语言，手把手教您把系统部署到云端。
> 
> 整个过程大约需要 **15-20 分钟**，按照下面的步骤操作即可。

---

## 📋 部署前准备

在开始之前，您需要准备两样东西：

| 需要准备 | 说明 | 难不难 |
|---------|------|--------|
| GitHub 账号 | 存放代码的地方（免费） | 👶 很简单 |
| Render 账号 | 云服务器平台（免费） | 👶 很简单 |

**提示**：两个平台都是完全免费的，不需要绑定信用卡！

---

## 第一步：注册 GitHub 账号

GitHub 是全球最大的代码托管平台，相当于一个"代码网盘"。

### 1.1 打开 GitHub 注册页面
在浏览器地址栏输入：`https://github.com`

### 1.2 填写注册信息
- **Email**：输入您的邮箱地址（建议用常用邮箱）
- **Password**：设置一个密码（至少8位）
- **Username**：设置一个用户名（英文，后面会用到，记下来）

### 1.3 验证邮箱
- 打开您的邮箱，找到 GitHub 发来的验证邮件
- 点击邮件中的 "Verify email address" 按钮

### 1.4 完成注册 ✅
注册成功后，您会看到 GitHub 的主页面。

---

## 第二步：上传代码到 GitHub

### 2.1 创建新仓库

1. 登录 GitHub 后，点击页面右上角的 **"+"** 按钮
2. 选择 **"New repository"**（新建仓库）

### 2.2 设置仓库信息

在创建仓库页面填写：

| 字段 | 填什么 | 示例 |
|------|--------|------|
| Repository name | 给仓库起个名字 | `icp-plan-system` |
| Description | 简单描述（可不填） | `内控计划共享编辑系统` |
| Public/Private | 选择 **Public** | 公开（免费必须选这个） |

**重要**：不要勾选 "Add a README file"，因为代码包里可能已经有了。

### 2.3 确认创建
点击 **"Create repository"** 按钮。

### 2.4 上传代码文件

创建仓库后，您会看到一个页面，按照以下步骤操作：

1. 找到 **"uploading an existing file"** 链接，点击它
2. 把项目代码文件夹里的**所有文件和文件夹**拖到上传区域
3. 确保上传了这些关键文件：
   - `package.json`
   - `server.js`
   - `public/` 文件夹
   - `db/` 文件夹
   - `routes/` 文件夹
   - `middleware/` 文件夹
   - `utils/` 文件夹
   - `Procfile` ✓
   - `render.yaml` ✓
4. 点击 **"Commit changes"** 按钮确认上传

**提示**：不要上传 `node_modules` 文件夹，那个太大了！

### 2.5 确认上传成功 ✅
刷新页面，您应该能看到所有文件都在仓库里了。

---

## 第三步：注册 Render 账号

Render 是一个提供免费云服务器的平台，可以运行 Node.js 应用。

### 3.1 打开 Render 官网
在浏览器地址栏输入：`https://render.com`

### 3.2 注册方式
- 点击 **"Get Started"`** 或 **"Sign Up"**
- 可以用 **GitHub 账号直接登录**（最方便！）
- 也可以用邮箱注册

### 3.3 授权 GitHub（如果用 GitHub 登录）
- 弹出窗口中点击 **"Authorize render-com"**
- 可能需要输入 GitHub 密码确认

### 3.4 完成注册 ✅
登录后，您会看到 Render 的控制台页面。

---

## 第四步：创建 Web 服务

现在把 GitHub 上的代码部署到 Render 上。

### 4.1 新建 Web Service

1. 在 Render 控制台，点击左侧菜单 **"Blueprints"**
2. 点击 **"New Blueprint Instance"**

### 4.2 连接 GitHub 仓库

1. 第一次使用需要授权 GitHub：
   - 点击 **"Connect GitHub"**
   - 在弹出窗口中选择刚才创建的仓库
   - 点击 **"Install"** 安装

### 4.3 配置部署

在 Blueprint 配置页面：

| 字段 | 填什么 |
|------|--------|
| GitHub Repo | 选择您的仓库（如 `your-username/icp-plan-system`） |
| Branch | 保持默认 `main` |
| Region | 选择 `Singapore` 或离您近的区域 |

### 4.4 确认部署

点击 **"Apply"** 或 **"Create"** 按钮开始部署。

### 4.5 等待部署完成

- 页面会显示部署进度
- 等待 **2-5 分钟**
- 当看到绿色 ✅ 或状态变成 "Live" 时，说明部署成功！

---

## 第五步：获取访问地址

### 5.1 找到您的网站地址

1. 点击刚创建的服务名称
2. 在页面顶部找到 **"URL"** 或 **"Your service is live at:"**
3. 复制那个网址链接

### 5.2 打开系统

1. 在浏览器中打开刚才复制的链接
2. 如果看到登录页面，说明部署成功！🎉

### 5.3 登录测试

使用默认测试账号登录：

| 账号类型 | 用户名 | 密码 |
|---------|--------|------|
| 管理员 | admin | admin123 |
| 组长 | manager | manager123 |
| 成员 | user01 | user123 |

---

## ⚠️ 重要提醒：免费版的数据限制

### 免费版的特点

Render 的免费版有一个限制：**服务器重启后数据会丢失**

| 情况 | 说明 |
|------|------|
| 🔄 服务器重启 | Render 免费版每天会自动重启一次 |
| 💾 数据丢失 | 重启后，所有录入的计划数据会被清空 |
| ✅ 账号保留 | 用户账号和小组信息不受影响 |

### 适合的使用场景

免费版**非常适合**：

- ✅ 体验和测试系统功能
- ✅ 向领导演示系统
- ✅ 小范围内部试用
- ✅ 开发调试

### 如果需要永久保存数据

有两个选择：

#### 方案一：升级 Render 付费版
- 费用：$7/月（约50元人民币）
- 优点：数据永久保存，无需自己维护
- 操作：在 Render 控制台点击 "Upgrade" 升级

#### 方案二：在自己电脑本地运行
- 费用：免费
- 优点：数据永远在您自己电脑上，不会丢失
- 操作：
  1. 安装 Node.js（https://nodejs.org）
  2. 双击运行 `start.bat` 或在命令行输入 `npm start`
  3. 打开浏览器访问 `http://localhost:3000`

---

## 🔧 常见问题解答

### Q1：部署后显示 "Service Not Found"？
**答**：稍等1-2分钟，Render 还在启动中。刷新页面再试。

### Q2：页面打不开，显示错误？
**答**：
1. 查看 Render 控制台的日志（点击服务名称 → "Logs"）
2. 把错误信息复制下来搜索解决方案

### Q3：忘记了网站地址怎么办？
**答**：登录 Render 控制台，点击您的服务，URL 在页面顶部。

### Q4：能绑定自己的域名吗？
**答**：免费版不支持自定义域名。升级付费版后可以绑定。

### Q5：如何更新网站内容？
**答**：
1. 修改本地代码
2. 上传到 GitHub 同一仓库
3. Render 会自动检测到更新并重新部署

---

## 📞 需要帮助？

如果在部署过程中遇到问题，可以：
1. 查看 Render 官方文档：https://render.com/docs
2. 搜索错误信息寻找解决方案
3. 截图错误页面，向懂技术的朋友求助

---

**祝您部署成功！🎊**
